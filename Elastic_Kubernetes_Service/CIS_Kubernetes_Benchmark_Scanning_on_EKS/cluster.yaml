apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: kube-bench-lab       # Replace with $CLUSTER_NAME
  region: us-east-1          # Replace with $AWS_REGION
  version: "1.27"
managedNodeGroups:
  - name: ng-1
    instanceType: t3.medium
    desiredCapacity: 2